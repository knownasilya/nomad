// nomad.fs manifest — re-exported from the shared canonical source so desktop and mobile agree on
// the API surface by construction (ADR-0010). Edit the method list in shared/fs-manifest.mjs.
export { default } from '../../../../../shared/fs-manifest.mjs';
